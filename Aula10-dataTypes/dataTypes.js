// Data Types
// string, number, undefined, null, boolean, symbol

const nome = 'Kayne'; // String
const nome1 = "Kayne"; // String
const nome2 = `Kayne`; // String

const num1 = 10; // Integer
const num2 = 10.52; // Float

let nomeAluno; // Undefined
const sobrenomeAluno = null; // Null

const aprovado = true; // Boolean (true/false)

console.log(typeof(num1), num1); // Exibe o tipo da variável
console.log(typeof(aprovado), aprovado); 
console.log(typeof(nome), nome); 